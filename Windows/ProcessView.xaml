<UserControl x:Class="GarterBelt.Windows.ProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GarterBelt.Windows"
             mc:Ignorable="d" 
             Background="white"
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <StackPanel Orientation="Horizontal" Height="24">
            <CheckBox x:Name="ProcessEnabled" VerticalAlignment="Center" Margin=" 0, 0, 10, 0"/>
            <Grid Background="Black" Width="24">
                <Image x:Name="ProcessIconImage"/>
            </Grid>
            <Label x:Name="ProcessName" VerticalAlignment="Center" Content="Process Name"/>
        </StackPanel>
    </Grid>
</UserControl>
